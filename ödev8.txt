CREATE TABLE employee(
	id INTEGER, 
	name VARCHAR(50), 
	birthday DATE, 
	email VARCHAR(100)
);

UPDATE employee
SET name = 'Philbert',
	Birthday = '1903-04-12',
	email = 'pdunton1@vinaora.com'
WHERE id = 3
RETURNING *;

DELETE FROM employee
WHERE id = 3
RETURNING *;